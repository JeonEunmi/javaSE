package com.test038;

public class Main {

	public static void main(String[] args) {

		// Test 클래스의 객체(instance) 생성
		// Test 클래스의 private 생성자 때문에 객체(instance) 생성 불가
		/*Test t1 = new Test(); // The constructor Test() is not visible

		// 하위 (instance) 멤버 접근 불가
		System.out.println(t1.getA());

		// Math 클래스의 private 생성자 때문에 객체(instance) 생성 불가
		// Math m = new Math();

		// Math 클래스의 객체 생성 없이 하위 (static) 멤버 접근 가능
		System.out.println(Math.random());*/
	}

}
