package com.test;

// 선언된 클래스는 메모리(heap영역) 적재가 되어야 활동 가능
// -> 객체(instance) 생성 과정
// -> new 연산자
public class Test006 {

	// 객체 생성시 내부적으로 생성자가 자동 호출된다.
	// ->멤버 구성이 아무것도 없는 경우 기본 생성자 자동 제공
	
}
